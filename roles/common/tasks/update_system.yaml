- name: Update system with dnf
  become: true
  block:

  - name: Update all packages
    ansible.builtin.dnf:
      name: "*"
      state: latest

  - name: Cleanup unneeded packages
    ansible.builtin.dnf:
      autoremove: yes
